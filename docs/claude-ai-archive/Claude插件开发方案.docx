**Claude 网页版**

**任务管理与自动执行插件**

完整开发方案

版本 1.0 \| 2025 年

浏览器插件 · 自动化调度 · DOM 注入 · 任务编排

**一、项目概览**

**1.1 项目背景与目标**

Claude
网页版（claude.ai）目前缺乏任务自动化能力，用户每次交互均需手动触发。本插件旨在通过浏览器扩展的方式，在不修改
Claude
官方代码的前提下，为其赋予\"任务管理\"与\"自动执行\"两大核心能力，使其成为可定时、可编排、可无人值守运行的智能工作流平台。

  -----------------------------------------------------------------------
  **核心目标**

  ✦ 任务管理：支持创建、编辑、暂停、删除多任务，并可视化管理任务状态

  ✦ 自动执行：按 Cron 计划、链式依赖或外部触发方式自动驱动 Claude 对话

  ✦ 多步编排：支持含变量插值的多步骤任务流，步骤间可传递上下文

  ✦ 零侵入部署：仅通过浏览器扩展注入，无需修改 claude.ai 任何源码
  -----------------------------------------------------------------------

**1.2 插件定位与边界**

本插件作为 claude.ai 的\"外挂自动化层\"，其能力边界清晰：

-   插件负责：触发时机控制、消息输入注入、响应内容采集、任务状态持久化

-   Claude 负责：实际的 AI 推理、内容生成、工具调用等核心智能行为

-   不涉及：账号系统、计费逻辑、API Key 管理、跨账号数据同步

**二、系统架构设计**

**2.1 整体分层架构**

插件采用四层架构，各层职责清晰、单向依赖：

  ---------- ------------------------------------------------------------
  **第四层 · 侧边任务面板、弹出配置页、任务编辑器、执行日志面板
  UI 层**    

  **第三层 · Cron 调度器、任务队列、链式触发、Webhook 接收器
  调度层**   

  **第二层 · 步骤执行器、变量引擎、动作注册表、错误恢复机制
  执行层**   

  **第一层 · DOM Bridge、输入注入、输出监听、页面健康检测
  桥接层**   
  ---------- ------------------------------------------------------------

**2.2 核心模块说明**

**DOM Bridge（页面桥接层）**

作为插件与 claude.ai
页面交互的唯一通道，负责：输入框内容写入、发送按钮触发、响应完成检测、对话内容提取。使用
MutationObserver 监听页面 DOM 变化，通过语义化 CSS
选择器定位元素，具备选择器失效告警机制。

**Task Scheduler（调度引擎）**

运行于 Service Worker
中，生命周期独立于页面标签页。支持三种触发模式：Cron 定时（基于
cron-parser）、任务完成后链式触发、外部 Webhook
事件触发。多标签冲突问题通过 BroadcastChannel
主从选举机制解决，同一账号同一时刻仅有一个\"主调度实例\"。

**Step Executor（步骤执行器）**

负责逐步执行任务中的各个步骤，支持 Claude
对话步骤与原生动作步骤两类。使用 Handlebars 风格的 {{变量名}}
语法在步骤间传递上下文数据，支持条件分支跳转（基于关键词匹配或 JSONPath
提取）。

**Task Store（持久化存储）**

基于 IndexedDB 实现，存储任务定义、执行历史、变量快照三类数据。设计为
append-only 执行日志，支持任意历史回溯。通过 chrome.storage.sync
将任务配置同步到用户账号，实现多设备共享。

**2.3 数据流向**

完整的任务执行数据流如下：

1.  调度器按触发条件唤醒任务，从 IndexedDB 读取任务定义与上次执行上下文

2.  步骤执行器对当前步骤的 Prompt 模板进行变量插值，生成最终消息内容

3.  DOM Bridge 将消息注入 claude.ai 输入框，模拟用户输入并触发发送

4.  输出监听器等待 Claude 流式输出结束（检测停止按钮消失 +
    文本稳定双判断）

5.  响应内容写入当前步骤的 outputVar，更新执行上下文，触发 UI 进度刷新

6.  若为最后步骤，记录执行日志，更新任务状态，按配置决定是否触发后续任务

**三、功能模块规划**

**3.1 功能优先级矩阵**

依据开发成本与用户价值进行优先级划分：

+-----------------------------------+-----------------------------------+
| **P0 必须上线（MVP）**            | **P1 重要但可迭代**               |
+-----------------------------------+-----------------------------------+
| • 任务 CRUD（创建 / 查看 / 删除） | • 多步骤任务与变量传递            |
|                                   |                                   |
| • Cron 定时触发                   | • 链式任务触发                    |
|                                   |                                   |
| • 单步骤自动发送与采集            | • 执行历史与回放                  |
|                                   |                                   |
| • 任务执行状态可视化              | • 任务导入 / 导出                 |
|                                   |                                   |
| • DOM 失效检测告警                | • 失败自动重试与退避策略          |
+-----------------------------------+-----------------------------------+
| **P2 差异化增值特性**             | **P3 远期规划**                   |
+-----------------------------------+-----------------------------------+
| • 条件分支步骤                    | • 多账号任务隔离                  |
|                                   |                                   |
| • Webhook 外部触发                | • 可视化流程编排画布              |
|                                   |                                   |
| • 结果推送（邮件 / 飞书 / Slack） | • 跨平台（支持其他 AI 产品）      |
|                                   |                                   |
| • 任务模板市场                    | • 团队协作共享任务库              |
+-----------------------------------+-----------------------------------+

**3.2 用户界面规划**

**侧边任务面板（主界面）**

固定在 claude.ai 右侧，宽 320px，通过 Shadow DOM
隔离样式，包含：任务列表（状态指示灯、触发时间、操作按钮）、实时执行进度条、快捷新建入口。折叠时仅显示一个浮动图标，不干扰正常使用。

**任务编辑器（抽屉式弹出）**

点击新建或编辑任务时从右侧滑出，包含：任务基本信息（名称、描述）、触发器配置（Cron
表达式可视化选择器）、步骤列表（可拖拽排序）、每步骤的 Prompt
模板编辑器（支持变量高亮提示）。

**执行历史面板**

展示任务的历史执行记录，每条记录包含执行时间、耗时、状态、各步骤输出摘要，支持展开查看完整输出内容，并可一键将某次执行结果复制到剪贴板。

**扩展弹出页（Popup）**

点击浏览器工具栏图标时展示，提供全局开关（启用/禁用所有任务）、快速查看当天待执行任务、数据导出备份功能，以及跳转到完整设置页的入口。

**四、分阶段开发计划**

**4.1 开发阶段总览**

项目共分四个阶段，总工期约 12 周，每阶段以可用的可交付物作为验收依据：

+--------------+-------------------------------------------------------+
| **Phase 1**  | **DOM Bridge 与插件框架搭建** ⏱ 第 1--2 周            |
|              |                                                       |
| 基础建设     | *目                                                   |
|              | 标：完成页面注入能力，验证自动发送与响应采集的可行性* |
+--------------+-------------------------------------------------------+
| 任务 1       | 创建 Chrome MV3                                       |
|              | 插件项目骨架（Manifest、目录结构、构建脚本）          |
+--------------+-------------------------------------------------------+
| 任务 2       | 实现 DOM                                              |
|              | Bridge：输入框写入、发送触发、流式输出完成检测        |
+--------------+-------------------------------------------------------+
| 任务 3       | 编写 DOM 健康检测模块：选择器自检，失效时发出 badge   |
|              | 告警                                                  |
+--------------+-------------------------------------------------------+
| 任务 4       | 完成手动执行单条 Prompt 的端到端 Demo，录制验证视频   |
+--------------+-------------------------------------------------------+

+--------------+-------------------------------------------------------+
| **Phase 2**  | **任务管理核心与基础 UI** ⏱ 第 3--5 周                |
|              |                                                       |
| 核心功能     | *目标：完成 P0 功能，用户可创建并手动运行多步骤任务*  |
+--------------+-------------------------------------------------------+
| 任务 1       | 设计任务数据结构（Schema），实现 IndexedDB            |
|              | 存储层（CRUD + 历史日志）                             |
+--------------+-------------------------------------------------------+
| 任务 2       | 实现步骤执行器：多步串行、变量插值                    |
|              | {{var}}、基础错误捕获                                 |
+--------------+-------------------------------------------------------+
| 任务 3       | 实现 Cron 调度器（Service Worker + chrome.alarms      |
|              | 双保险，处理休眠唤醒）                                |
+--------------+-------------------------------------------------------+
| 任务 4       | 开发侧边任务面板 UI（Shadow DOM                       |
|              | 隔离），含任务列表与手动执行按钮                      |
+--------------+-------------------------------------------------------+
| 任务 5       | 开发任务编辑器（基础版：名称 / Cron / 步骤列表）      |
+--------------+-------------------------------------------------------+
| 任务 6       | 集成通知系统（chrome.notifications，任务完成 /        |
|              | 失败推送）                                            |
+--------------+-------------------------------------------------------+

+--------------+-------------------------------------------------------+
| **Phase 3**  | **高级执行能力与体验优化** ⏱ 第 6--9 周               |
|              |                                                       |
| 高级特性     | *目标：完成 P1                                        |
|              | 功能，插件可作为稳定的日常自动化工具使用*             |
+--------------+-------------------------------------------------------+
| 任务 1       | 实现                                                  |
|              | 失败重试机制：指数退避、最大重试次数、错误分类（网络  |
|              | / DOM / 限流）                                        |
+--------------+-------------------------------------------------------+
| 任务 2       | 实现链式任务触发：任务 A 完成后可自动触发任务         |
|              | B，支持条件激活                                       |
+--------------+-------------------------------------------------------+
| 任务 3       | 实现 Webhook 接收器：本地监听，支持外部系统 POST      |
|              | 触发任务                                              |
+--------------+-------------------------------------------------------+
| 任务 4       | 完善执行历史面板：每步输出可展开，支持重放单次执行    |
+--------------+-------------------------------------------------------+
| 任务 5       | 实现任务导入 / 导出（JSON 格式），支持跨设备迁移      |
+--------------+-------------------------------------------------------+
| 任务 6       | 新对话模式：执行前自动新建对话，避免历史上下文污染    |
+--------------+-------------------------------------------------------+
| 任务 7       | chrome.storage.sync 任务配置同步（含冲突合并策略）    |
+--------------+-------------------------------------------------------+

+--------------+-------------------------------------------------------+
| **Phase 4**  | **发布准备与生态建设** ⏱ 第 10--12 周                 |
|              |                                                       |
| 发布上线     | *目标：完成 Chrome Web Store 发布，建立持续维护机制*  |
+--------------+-------------------------------------------------------+
| 任务 1       | P2 差异化功能开发：条件分支、结果推送集成（飞书 /     |
|              | Slack Webhook）                                       |
+--------------+-------------------------------------------------------+
| 任务 2       | 完整测试：DOM 兼容性测试（claude.ai                   |
|              | 多种布局）、并发任务压测、数据恢复测试                |
+--------------+-------------------------------------------------------+
| 任务 3       | 安全审计：XSS 防护、Prompt                            |
|              | 注入风险评估、权限最小化审查                          |
+--------------+-------------------------------------------------------+
| 任务 4       | 撰写用户文档、内置新手引导 Onboarding 流程            |
+--------------+-------------------------------------------------------+
| 任务 5       | 准备 Chrome Web Store 发布素材，通过 Manifest V3 审核 |
+--------------+-------------------------------------------------------+
| 任务 6       | 建立 DOM 选择器监控告警（GitHub Action                |
|              | 定期巡检），维护快速热修复流程                        |
+--------------+-------------------------------------------------------+

**五、关键里程碑**

**5.1 交付里程碑**

  ---------------------- -------------- --------------------------------------------------------
  **里程碑**             **交付时间**   **验收标准**

  **M1 DOM Bridge Demo** 第 2 周末      可自动向 Claude 发送指定消息并提取完整响应，手动演示无误

  **M2 MVP 内部可用**    第 5 周末      支持创建 Cron 任务、多步骤执行、手动触发，核心功能闭环

  **M3 Beta 版本**       第 9 周末      链式任务、重试机制、历史记录完整可用，内测用户反馈覆盖

  **M4 Chrome Store      第 12 周末     通过 Google 审核，公开上架，含用户文档与更新机制
  发布**                                
  ---------------------- -------------- --------------------------------------------------------

**5.2 每周节奏**

每周采用固定节奏推进开发：

-   周一：确认本周任务范围，评估技术依赖与风险点

-   周三：中期检查，记录 DOM 选择器当前状态快照（用于兼容性对比）

-   周五：完成当周任务，更新进度文档，推送可演示的 build

-   持续：claude.ai 页面变更监控，每天运行自动化巡检脚本检测关键选择器

**六、技术选型**

**6.1 技术栈**

  -------------- --------------------- ---------------------------------------------------------
  **层级**       **技术 / 工具**       **选型理由**

  **插件框架**   Chrome MV3（Manifest  官方推荐标准，Service Worker 支持更好的后台调度能力
                 V3）                  

  **前端框架**   React + TypeScript    组件化 UI 开发，类型安全降低运行时错误，生态成熟

  **构建工具**   Vite + CRXJS 插件     极速热更新，自动处理 MV3 资源分包，开发体验优秀

  **样式隔离**   Shadow DOM + CSS      彻底隔离插件样式与 claude.ai 页面样式，互不干扰
                 Modules               

  **持久化**     IndexedDB（idb        浏览器原生，无需额外服务，支持复杂查询与事务
                 封装库）              

  **Cron 解析**  cron-parser           轻量、无依赖，精确解析标准 Cron 表达式计算下次执行时间

  **数据同步**   chrome.storage.sync   免服务端实现多设备同步，限额内（100KB）足够任务配置存储

  **测试框架**   Vitest + Playwright   单元测试用 Vitest，端到端模拟真实浏览器行为用 Playwright
  -------------- --------------------- ---------------------------------------------------------

**6.2 关键技术决策说明**

**为什么选择 MV3 而非 MV2？**

MV2 将于 2025 年底被 Chrome 强制停用。MV3 的 Service Worker
背景脚本虽然生命周期不持久，但通过 chrome.alarms API
可实现分钟级精度的任务唤醒，足以满足绝大多数定时需求。对于需要持续监听的场景，引入
Keepalive 心跳机制维持 Service Worker 活跃。

**Shadow DOM 隔离的取舍**

Shadow DOM 能彻底隔离样式，防止 claude.ai 页面的全局 CSS 污染插件
UI。代价是事件冒泡机制略有差异，部分需要全局捕获的事件需要手动处理。这一代价在可控范围内，隔离收益远大于成本。

**不使用官方 API 的原因**

直接调用 Anthropic API 需要用户提供 API Key 并产生额外费用，且与
claude.ai 的会话历史、Projects、工具等功能割裂。DOM Bridge
方案虽然有选择器脆弱性，但用户体验更统一，且可利用 claude.ai
的全部功能（如联网搜索、代码执行等）。

**七、风险识别与管理**

**7.1 风险矩阵**

  ---------------------- -------------- ------------ ----------------------------------------------------------------------------------
  **风险项**             **影响程度**   **可能性**   **应对措施**

  claude.ai 界面重构导致 **高**         高           建立选择器监控巡检（CI
  DOM 选择器失效                                     每日自动化测试），维护备用选择器列表，插件内置自检模式，失效时立即告警并暂停任务

  Chrome 限制 Service    **中**         中           结合 chrome.alarms 唤醒 + Keepalive
  Worker 运行时间                                    心跳双保险，关键调度逻辑设计为幂等可重入，任务状态持久化防止丢失

  Anthropic              **高**         低           控制执行频率（步骤间随机延迟 3--8
  封禁自动化行为                                     秒），拟人化输入节奏，避免高并发请求；同时准备基于官方 API 的备选方案

  多标签页并发执行冲突   **中**         中           BroadcastChannel
                                                     实现主从选举，同账号同时仅一个\"主实例\"执行任务，其余实例监听状态同步

  IndexedDB              **低**         低           每次执行前增量备份到 chrome.storage.local，提供手动导出 JSON
  数据损坏或丢失                                     功能，文档指导用户定期备份

  Google 审核拒绝上架    **中**         中           严格遵守 MV3 权限声明规范，申请最小权限集合，提前阅读审核指南并参考类似产品，预留
                                                     2 周审核缓冲期
  ---------------------- -------------- ------------ ----------------------------------------------------------------------------------

**7.2 技术债务管理策略**

-   DOM Bridge
    的选择器列表单独维护在配置文件中，热更新无需重新发布整个插件（利用
    remote config）

-   每个 Phase 末进行代码审查，专项处理当期引入的 TODO
    与临时方案，避免债务累积

-   关键模块（调度器、步骤执行器）保持 80%
    以上单元测试覆盖率，重构有安全网

**八、团队与资源规划**

**8.1 推荐团队配置**

  -------------------- ------------- ------------------------------------------------------
  **角色**             **人数**      **核心职责**

  **前端开发工程师**   1--2 人       DOM Bridge 开发、插件 UI 实现、构建工程维护、Chrome
                                     审核对接

  **全栈 / 扩展专家**  1 人          Service Worker 架构、调度引擎、IndexedDB 存储层设计

  **产品 / 设计**      0.5 人        用户体验设计（兼职）、功能优先级决策、用户反馈收集

  **QA 工程师**        0.5 人        端到端自动化测试、兼容性测试、发布前回归测试（兼职）
  -------------------- ------------- ------------------------------------------------------

**8.2 单人开发可行性评估**

如果由单人独立开发，建议以下调整：

-   工期适当延长至 16--18 周，Phase 3 的 P1 功能可拆分到 Phase 4 前期

-   P2 功能（Webhook、条件分支）推迟到 v1.1，确保 v1.0 功能质量

-   UI 设计借助 shadcn/ui 或类似组件库提升效率，避免从零实现组件

-   测试重点覆盖 DOM Bridge 和调度器，其余模块以手动测试为主

**九、成功指标与验收标准**

**9.1 技术验收标准**

  -----------------------------------------------------------------------
  **功能完整性（必须全部满足）**

  ① 任务在指定 Cron 时间（误差 ≤ 1 分钟）自动触发执行

  ② 多步骤任务中，前步骤变量可被后步骤正确引用（变量插值 100% 准确）

  ③ DOM Bridge 在 claude.ai 界面正常情况下，消息发送成功率 ≥ 99%

  ④ 任务执行历史完整持久化，浏览器重启后数据不丢失

  ⑤ 多标签页打开时，任务不重复执行（主从选举机制生效）
  -----------------------------------------------------------------------

  -----------------------------------------------------------------------
  **性能基准（建议满足）**

  • 插件注入后页面首次渲染性能损耗 \< 50ms

  • 侧边面板打开响应时间 \< 200ms

  • IndexedDB 单次读写操作 \< 20ms（P99）

  • Service Worker 唤醒到任务开始执行延迟 \< 5 秒
  -----------------------------------------------------------------------

**9.2 用户体验验收标准**

-   新用户在无文档帮助下，可在 10 分钟内完成第一个定时任务的创建与运行

-   任务执行失败时，用户能在 30 秒内通过通知了解到失败原因与建议处理方式

-   任务面板的存在不影响用户正常使用 claude.ai 的任何功能

-   插件卸载时不在 claude.ai 页面留下任何残留的 DOM 元素或全局变量

**十、后续演进路线**

**v1.1 体验完善（发布后 4--6 周）**

-   可视化 Cron 编辑器（日历 + 滑块形式，无需手写 Cron 表达式）

-   任务模板库：内置 10--20
    个常用任务模板（每日摘要、代码审查、周报生成等）

-   条件分支步骤：支持基于关键词匹配决定跳转到哪个后续步骤

**v1.2 集成扩展（发布后 3--4 个月）**

-   结果推送集成：飞书、Slack、钉钉 Webhook，任务完成后自动发送摘要

-   Webhook 触发端点：允许外部系统（如 n8n、Zapier）触发插件任务

-   支持 Firefox（WebExtension API 适配），覆盖更多用户群体

**v2.0 平台化愿景（长期）**

-   可视化流程编排画布（节点连线方式定义任务流）

-   扩展支持其他 AI 产品页面（ChatGPT、Gemini 等），成为通用 AI
    自动化平台

-   团队版：任务库共享、执行权限管理、集中日志监控

*本方案以\"可持续维护\"为核心设计原则------*

*在 claude.ai 快速迭代的产品环境中，*

*插件的长期价值取决于监控、告警与快速修复能力，而非一次性的完美实现。*
